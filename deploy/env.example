# ppt-gen runtime environment
PORT=18080
DEBUG=0

# Optional storage overrides (else config.yml is used)
# STORE=json
# DATA=data/app.json
# DSN=postgres://user:pass@127.0.0.1:5432/pptgen?sslmode=disable
